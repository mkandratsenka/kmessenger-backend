app:
  actuator:
    security:
      username: actuator
      password: actuatorLocal

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true